Start with a clean pipeline: install → lint → test → build → deploy.

Cache dependencies to speed up builds, and run tests on pull requests.

Finally, deploy build artifacts to your host (GitHub Pages, Vercel, Netlify) on main branch merges.
