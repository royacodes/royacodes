We’ll build a reliable animation system using variants, layout animations, and scroll-based reveals.

The key is restraint: animate hierarchy, not everything — keep motion purposeful.

You’ll leave with reusable patterns for buttons, cards, and page transitions.
