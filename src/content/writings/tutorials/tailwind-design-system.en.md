## Step 1
Define your design tokens (colors, spacing, radius) in Tailwind’s `theme.extend`.

## Step 2
Create a small set of primitives (Button, Input, Card) that cover 80% of UI needs.

## Step 3
Document usage rules and accessibility defaults so the system stays consistent over time.
